function createTaskQuery(name, description, completed, goalid) {
    // do something here
}

function createGoalQuery(name, description,startdate,enddatecompleted,userid) {
    // do something here
}

function registerUserQuery(name, description, completed, goalid) {
    // do something here
}

exports.helpers = {
    createGoalQuery,
    createTaskQuery,
    registerUserQuery,
}

